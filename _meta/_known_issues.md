# Known Issues / Lifecycle Gaps

更新: YYYY-MM-DD

本文登记 COL5 文件生命周期缺口和跨列治理问题，不是当前任务队列。当前任务真源仍是 `_wiki-methodology/_top/_task_plan.md`。

## 格式

```markdown
## <ID>

现象:<发生了什么>

判断:<为什么属于 COL5 / 治理缺口,而不是当前 task>

处理原则:<保留 / 归档 / 注册 / 后续审计方式>
```

## 当前登记

✅ 项目刚初始化,暂无已知生命周期缺口。

## 处理完成后

- 修复过程和证据写 `_wiki-methodology/_top/_findings/` 或 `_wiki-methodology/_top/_logs/`。
- 若形成长期规则,升级到 `_meta/_naming.md`、项目 rule 或 share-top skill。
- 不回写 `_codemap.md` 的 current 摘要。
