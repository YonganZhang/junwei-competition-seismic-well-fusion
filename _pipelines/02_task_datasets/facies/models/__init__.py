"""Facies models dynamically discovered as ``models.<registered_name>``.

No centralized imports belong here.  Adding ``models/new_name.py`` with a
matching ``@register_model("new_name")`` is sufficient.
"""
